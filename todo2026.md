# 2026 架構演進計畫：多頻道配置化 (Multi-Variant Configuration)

## 目標
將目前的單一 Workflow 重構為支援多頻道（如科技、經濟、政治）的配置化架構，採用策略模式 (Strategy Pattern) 實現邏輯與配置分離。

## 核心原則
**「邏輯統一，配置分離」**
- `workflow/index.ts` 維持單一份，作為核心執行引擎。
- `workflow/config.ts` 定義不同頻道的差異化設定。

## 檔案結構規劃

```text
workflow/
├── index.ts        <-- (核心) 負責排程、API 呼叫、狀態管理
├── config.ts       <-- (新增) 定義 Variants 配置表
└── prompts/        <-- (新增資料夾)
    ├── index.ts    <-- 匯出點
    ├── common.ts   <-- 共用的 System Prompts (如 intro 格式)
    ├── tech.ts     <-- 科技版 Prompt (Cordelia & David)
    └── economy.ts  <-- 經濟版 Prompt (例如：Warren & Buffett)
```

## 實作細節

### 1. 定義配置介面 (`workflow/config.ts`)

```typescript
export interface VariantConfig {
  // 該頻道要抓取的來源與數量限制
  sourceLimits: {
    'hacker-news'?: number
    'bloomberg'?: number
    'reddit-investing'?: number
    // ...
  }
  // 該頻道的 Prompt 設定
  prompts: {
    script: string    // 主持人風格與對話指令
    blog: string      // 博客生成指令
    intro: string     // 簡介生成指令
  }
  // 該頻道的音訊設定 (可選)
  audio?: {
    speed?: number
    voices?: { male: string, female: string }
  }
}

export const VARIANTS: Record<string, VariantConfig> = {
  'hacker-news': {
    sourceLimits: { 'hacker-news': 5, 'github-trending': 2, ... },
    prompts: TechPrompts
  },
  'economy': {
    sourceLimits: { 'bloomberg': 5, 'yahoo-finance': 5 },
    prompts: EconomyPrompts
  }
}
```

### 2. 重構執行引擎 (`workflow/index.ts`)

修改 `PodcastScriptWorkflow` 的 `run` 方法：

1.  **讀取 Variant**: 從 event payload 取得 `variant` 參數。
2.  **載入配置**: `const config = VARIANTS[variant]`。
3.  **動態抓取**: 根據 `config.sourceLimits` 決定呼叫哪些爬蟲函數 (需要在 `utils.ts` 擴充工廠模式)。
4.  **動態生成**: 將 `config.prompts.script` 傳入 LLM。

### 3. 新增經濟版 Prompt (`workflow/prompts/economy.ts`)

定義新的人設與關注點：
- **主持人 A (總經專家)**: 關注通膨、利率、政策影響。
- **主持人 B (市場交易員)**: 關注股價波動、財報細節、技術線型。

## 遷移步驟 (Migration Steps)

1.  [ ] 建立 `workflow/prompts/` 資料夾，將現有 prompt 移入 `tech.ts`。
2.  [ ] 建立 `workflow/config.ts`，並將 `TECH_VARIANT` 設定為預設值。
3.  [ ] 修改 `workflow/index.ts`，將原本 Hardcode 的變數改為從 Config 讀取。
4.  [ ] 測試現有 Hacker News 流程是否正常 (Regression Test)。
5.  [ ] 實作經濟版爬蟲 (需調研目標網站 API/RSS)。
6.  [ ] 新增經濟版 Config 並上線。
